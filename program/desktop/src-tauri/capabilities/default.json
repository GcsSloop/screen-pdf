{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the first demo.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
